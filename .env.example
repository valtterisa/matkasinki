# Copy to .env (git-ignored). Never commit real keys.

# --- Agents (Claude) ---
ANTHROPIC_API_KEY=
# Demo mode is ON by default (local sample plans, no HSL/API calls).
# Set to 1 only when you want the live AI agent:
# MATKASINKI_LIVE_AGENT=1

# --- Out-of-Office email (see FEATURES.md → Resend setup) ---
RESEND_API_KEY=
OOO_DEFAULT_TO=dangvuhaidang@gmail.com

# --- Travel data integrations ---
WEATHER_API_KEY=
CURRENCY_API_KEY=
GEOCODE_API_KEY=
# --- HSL / Digitransit (routing API + map tiles) ---
# Get a key at https://portal-api.digitransit.fi/
DIGITRANSIT_API_KEY=
# Aliases also accepted:
DIGITRANSIT_PRIMARY_KEY=
DIGITRANSIT_SECONDARY_KEY=
AI_ROUTE_MODEL=

# --- Auth / multiplayer (rivals + friend invites) ---
DATABASE_URL=
AUTH_SECRET=
